# homebrew-tap

Homebrew tap for installing `cliproxy-image-cli`.

## Install

```bash
brew tap noooob-coder/tap
brew install cliproxy-image-cli
```

Or install directly:

```bash
brew install noooob-coder/tap/cliproxy-image-cli
```
